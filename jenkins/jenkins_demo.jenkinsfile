node("server") {
  def builder
    
  checkout scm
  try {
    builder = load "jenkins/groovy_demo.groovy"          
    builder()          
  }
  catch (exec) {
    currentBuild.result = 'FAILURE'
    throw exec
  }
}

